# Progress — 2026-08-24

## Current step

Step 1 (Initial Planning) — documentation and repository bootstrap complete.
No build or run work has started.

## Completed work

- Read the full workflow pack (`workflow/00`–`07`) and cluster/example docs.
- Fetched and reviewed the task source page: ISC26 SCC OpenFOAM (SPUMA) task.
- Created root `APPLICATION.md` — SPUMA `0.2-v2412` (OpenFOAM-v2412 fork), CPU-only
  backend, source URL/revision, dependencies, `etc/prefs.sh` build config,
  case setup (`occDrivAerStaticMesh`, 65M mesh), run config, correctness
  metric (mean Cd over `[800,1000]` within ±1.5% of `0.2699`), headline metric
  (`ClockTime` after `Time = 1000`), and baseline commands.
- Created root `README.md` — concise project overview.

## Decisions

- Target the **CPU-only** SPUMA backend (no GPU tasks).
- Active clusters: **Aspire2a** (`ssh aspire2a`) and **Gadi** (`ssh gadi-hpcai`).

## Repository synchronization

- Local commit `f351b11` ("Initialize project: workflow pack, OpenFOAM (SPUMA)
  application overview, README") pushed to `origin/main` (GitHub).
- Aspire2a clone at `~/scratch/OpenFOAM-occDrivAerStaticMesh-Optimization`
  pulled to `main`, tracking `origin/main`, clean.
- Gadi clone at `~/OpenFOAM-occDrivAerStaticMesh-Optimization` pulled to `main`,
  tracking `origin/main`, clean.

## Configuration notes / blockers

- `workflow/00-General-SSH-Rules.md` still contains placeholders to complete
  before remote work (launcher, module policy, login-node policy) and one
  inconsistent cluster listing (Gadi SSH alias/remote path vs `AGENTS.md`).
  Resolve before relying on it for remote execution.
- Remote project root on Aspire2a: `/home/users/ntu/pham0094/scratch/OpenFOAM-occDrivAerStaticMesh-Optimization`.
- Remote project root on Gadi: `/home/397/qp8375/OpenFOAM-occDrivAerStaticMesh-Optimization`.

## Next action

Step 2 — prepare build and run directories. Inspect cluster modules
(`module avail` for gcc/openmpi), clone SPUMA `0.2-v2412` source on the login
node, and create `builds/build-scripts/<build_name>/` (README + PBS build/run
scripts) with the CPU `etc/prefs.sh` configuration. No remote build/run until
the placeholders in `00-General-SSH-Rules.md` are completed.