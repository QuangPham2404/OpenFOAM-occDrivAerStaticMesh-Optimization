# experiments

One directory per optimization run or run family.

Each run directory contains a `README.md`, one or more run PBS scripts, and an
`outputs/` directory. Raw PBS outputs remain here; extracted values go to
`results/`.

- `_template/` — starter template (README + `run.pbs`) to copy for a new run.