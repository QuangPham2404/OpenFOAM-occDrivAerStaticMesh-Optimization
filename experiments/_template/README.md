# experiments/_template

Copy this directory as `experiments/<run-name>/` to start a new run.

The README records the run purpose, successful binary path, compiler/MPI
environment, input/resource metadata, validation, and any runtime
error-patching attempts. Raw PBS output belongs in `outputs/`.